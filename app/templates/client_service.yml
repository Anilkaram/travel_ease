apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-client-service"
spec:
  selector:
    app: "{{ .Release.Name }}-client"
    track: stable
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80

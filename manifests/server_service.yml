apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-server-service"
spec:
  selector:
    app: "{{ .Release.Name }}-server"
  type : ClusterIP
  ports:
    - port: 80
      targetPort: 5000
